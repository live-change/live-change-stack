
export function empty() {
  /// No page view event on server
}

export default {
  pageView: empty, action: empty, form: empty,
  loadingStarted: empty, loadingDone: empty, loadingError: empty,
  workingStarted: empty, workingDone: empty, workingError: empty
}
